/**
 * Homework 4
 * Ryan Slipher
 * 9/22/16
 */

/*
 * Use a for loop and print out an alert with a message 
 * 5 times
 */

//for(i=0; i<5; i++)
//{
//	alert("Hello");
//}

/*
 * Use a prompt and get the name of the user.  
 * Now put out an alert saying ‘Hello <user>’ 
 * where user is their name.
 */

//var name;
//
//	name = prompt("what is your name" , "John");
//	alert("Hello " + name);

/*
 * Use a prompt to collect a number from the user.  
 * Now change it into an integer.  Now use a for loop and 
 * print an alert for the number of times equal to the 
 * number you collected from the user.  For example if you 
 * got the number 5 from the user then print out “Hello” 
 * 5 times
 */

//var numBer;
//numBer = prompt("Enter a number");
//var numBer= parseInt(userInput);
//for(i=0; i==userInput; i++)
//	{
//	alert("Hello")
//	}

/*
 * Copy and run this program and state what it printed 
 * and why it behaved like that
 */

/*
 *  the code did not print
 */

//for(i=0; i<5; i++)
//{
//	If(i<3)
//		{
//		continue;
//        }
//     alert("i is now = +", i);
//}

/*
 * Use a for loop and print out numbers only from 3 to 5
 */

for(i+0; i<5; i++)
{
	if(i>=3)
	{
		continue;
	}	
	alert("i is",i);
}
